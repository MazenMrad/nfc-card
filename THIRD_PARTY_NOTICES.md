# Third-party notices

This project uses `godot-cpp`. This component supplies the official C++ bindings for Godot GDExtension.

The `godot-cpp` component uses the MIT License. Read `native/godot-cpp/LICENSE.md` for its license text.

The compiled GDExtension uses Microsoft Windows system libraries. These libraries include `WinSCard.dll` and the Universal C Runtime.

This project does not distribute these Windows system libraries.
